# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# datos reales -- nunca deben subirse al repo
*.xlsx
*.xls
*.csv
_scratch/
# listas de correos para scripts/grant-app-access.mjs --file: son datos
# personales de empleados, no tienen por que quedar en el historial de git.
correos*.txt
*.emails.txt

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# ...excepto la plantilla sin credenciales, que sí se versiona para que
# cualquiera pueda levantar el proyecto sabiendo qué variables hacen falta.
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# claude code -- config local del agente, no es parte del proyecto
.claude/
